# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules
server/node_modules

# Build output
dist
dist-ssr

# Environment secrets — NEVER commit these
.env
.env.local
server/.env
*.local

# Keep example env files (no secrets)
!.env.example
!server/.env.example

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Backup SQL (has local credentials)
server/srinivasa_medicals_backup.sql

# Media uploads (user-uploaded files — not tracked in git)
server/media/
